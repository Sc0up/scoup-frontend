import styled from 'styled-components';
import { ReactComponent as SettingsSvg } from '../../../assets/images/icons/etc/settings.svg';

export default function Settings() {
  return <SettingsIcon />;
}

const SettingsIcon = styled(SettingsSvg)``;
