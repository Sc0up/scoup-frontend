import styled from 'styled-components';
import { ReactComponent as LogoSvg } from '../../../assets/images/icons/logo/logo.svg';

export default function Logo() {
  return <LogoIcon />;
}

const LogoIcon = styled(LogoSvg)``;
