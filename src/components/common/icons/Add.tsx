import styled from 'styled-components';
import { ReactComponent as AddSvg } from '../../../assets/images/icons/etc/add.svg';

export default function Add() {
  return <AddIcon />;
}

const AddIcon = styled(AddSvg)``;
