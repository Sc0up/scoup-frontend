import styled from "styled-components";

const ScheduleModal = () => {
  return <ScheduleModalWrapper></ScheduleModalWrapper>;
};

const ScheduleModalWrapper = styled.div``;

export default ScheduleModal;
