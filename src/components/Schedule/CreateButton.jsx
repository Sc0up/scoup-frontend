import styled from "styled-components";

const CreateButton = () => {
  return <CreateButtonWrapper></CreateButtonWrapper>;
};

const CreateButtonWrapper = styled.div``;

export default CreateButton;
