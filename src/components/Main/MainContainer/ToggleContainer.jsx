import styled from "styled-components";

const ToggleContainer = () => {
  return <ToggleContainerWrapper></ToggleContainerWrapper>;
};

const ToggleContainerWrapper = styled.div``;

export default ToggleContainer;
