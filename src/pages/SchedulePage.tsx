import styled from "styled-components";
import TempSchedule from "../components/Candidate/TempSchedule";

const SchedulePage = () => {
  return (
    <SchedulePageWrapper>
      <TempSchedule />
    </SchedulePageWrapper>
  );
};

const SchedulePageWrapper = styled.div``;

export default SchedulePage;
